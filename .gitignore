console.log("Debug: Iniciando processamento de feeGroupsByAccount com result:", result);

const feeGroupsByAccount = Object.keys(result).reduce((acc, accountNumber) => {
  console.log("Debug: Processando accountNumber:", accountNumber);
  const groupNames = Object.keys(result[accountNumber]);
  console.log("Debug: Grupos para accountNumber", accountNumber, ":", groupNames);
  
  groupNames.forEach((groupName) => {
    console.log("Debug: Adicionando grupo", groupName, "para accountNumber", accountNumber);
    acc.push(result[accountNumber][groupName]);
  });
  
  return acc;
}, []);

console.log("Debug: feeGroupsByAccount concluído:", feeGroupsByAccount);
