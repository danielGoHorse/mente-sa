import * as styles from "./styles";

<div className={styles.timelineWrapper}>
  <button className={`${styles.chevronButton} ${styles.chevronLeft}`} onClick={scrollLeft}>
    <ChevronLeft size={20} />
  </button>
  <button className={`${styles.chevronButton} ${styles.chevronRight}`} onClick={scrollRight}>
    <ChevronRight size={20} />
  </button>

  <div ref={scrollRef} className={styles.scrollContainer}>
    <div className={styles.scrollContent}>
      {/* timeline steps aqui */}
    </div>
  </div>
</div>
