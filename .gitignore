const _returnObj = {
  group: 'Fee Package',
  packages: listFee
    .filter(element => element.groupName !== 'ERA' && element.groupName !== 'EFRA') // Exclui 'ERA' e 'EFRA'
    .map(element => ({
      name: element.groupName,
      id: element.id
    }))
};

console.log(_returnObj);
