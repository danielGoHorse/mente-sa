interface DocumentUploadedProps {
  documentName: string;
  documentSize?: string;
  documentStatus?: string;
  documentUUID?: string;
  documentURL?: string;        // local blob (pode manter se quiser)
  documentURLPreview?: string; // local blob
  serverPath?: string;         // "uuid/file.ext" => vindo do backend
  serverUrlSigned?: string;    // signed URL (preview/download)
  uploadedBy: { fullName?: string; userId?: string };
  createdAt?: string | number | Date;
  itemRef?: string | undefined;
}
