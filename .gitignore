const exceptionGroup = groupedFees[FeeType].feeGroups.find(group => group.name === "Exceptions");
if (!exceptionGroup) {
  groupedFees[FeeType].feeGroups.push({
    name: "Exceptions",
    code: 3,
    fields: exceptions
  });
} else {
  exceptionGroup.fields.push(...exceptions);
}
