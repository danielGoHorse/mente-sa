#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Forçar o script a rodar no diretório do repositório
cd "$(git rev-parse --show-toplevel)" || exit 1

# run commit lint
npx commitlint --edit

# run eslint
yarn lint

# run tests
yarn test
