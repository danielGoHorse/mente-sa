SELECT 
    OBJECT_NAME(object_id) AS ProcedureName,
    ISNULL(CAST(IS_ENCRYPTED AS BIT), 0) AS IsEncrypted
FROM sys.objects
WHERE type = 'P' -- 'P' é o tipo de objeto para stored procedures
AND OBJECT_NAME(object_id) = 'sp_get_branch_feepackage_info_offr';
