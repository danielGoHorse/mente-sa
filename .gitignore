// src/app/(private)/Wire-Transfer---USD/[requestId]/page.tsx
import PageTitle from "@/components/PageTitle";
import PageTabs from "./page_tabs";

type PageProps = { params: { requestId?: string } };

export default async function Page({ params }: PageProps) {
  const { requestId } = await params; // 👈 satisfaz o Next

  return (
    <>
      <PageTitle title="Wire Transfer - USD" />
      <div className="flex min-h-screen flex-col items-center">
        <PageTabs
          params={{ requestName: "wire-transfer", requestId }}
          tabTriggerType="request"
        />
      </div>
    </>
  );
}
