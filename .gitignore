import { configureSchemaMongoToReturnId } from '#src/core/data/mongoSchemaBase.js'
import mongoose, { Schema } from 'mongoose'

export const EquityTeamSchema = new Schema(
  {
    userName: {
      type: String,
      required: true
    },
    userNameFull: {
      type: String,
      required: true
    },
    email: {
      type: String,
      requireid: true
    }
  },
  {
    collection: 'EquityTeam',
    timestamps: true
  }
)

configureSchemaMongoToReturnId('toObject', EquityTeamSchema)

export const EquityTeam = mongoose.model('EquityTeam', EquityTeamSchema)


E me parece que esse cara aqui a gente tbm não esta mais precisando

import { mssql } from "./mssql"

mongoose.Promise = global.Promise

export const startConnectionWithSql = ({ server, user, password, database }) => {
  const config = {
    server,
    user,
    password,
    database,
    options: {
      encrypt: false,
      trustServerCertificate: true,
    },
  };
  try {
    mssql.close();
    const connection = mssql.connect(config);
    console.log('Connect to SQL Server successfully');
    return connection;

  } catch (err) {
    console.log('Error connecting to SQL Server', err.message);
    throw err;
  }


}
