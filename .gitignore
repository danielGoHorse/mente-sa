import React from "react";

export default function TimelineApprovalsPopover({ data = [] }) {
  return (
    <div className="bg-white shadow-lg rounded-lg p-4 min-w-[300px]">
      <div className="font-semibold mb-2 text-[#484A55]">Approvals & Restrictions</div>
      <div className="space-y-2">
        {data && data.length > 0 ? (
          data.map((step, i) => (
            <div key={i} className="border-b pb-2 last:border-b-0">
              <div className="font-medium">{step.name}</div>
              <div className="text-xs text-[#636574]">Status: {step.status}</div>
              {/* Add more info if you want */}
            </div>
          ))
        ) : (
          <div className="text-sm text-[#636574]">No details.</div>
        )}
      </div>
    </div>
  );
}
