export function AuthProvider() {
  const setAuth = useAuthStore((s) => s.setAuth);   // função que marca isAuth=true
  // ...

  async function doLogin(/* params: IAuthParams */) {
    if (process.env.NEXT_PUBLIC_BYPASS_AUTH === 'true') {
      // ↙ força como logado sem bater no back-end
      setAuth(true);
      return;
    }

    /* === fluxo normal === */
    // await api.post('/login', {...})
    // setAuth(true/false)
  }

  return { doLogin, isLoading: false };
}


NEXT_PUBLIC_BYPASS_AUTH=true
