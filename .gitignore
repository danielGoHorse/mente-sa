import React from "react";
import Timeline from "@/components/Timeline";
import { TimelinePopover } from "@/components/TimelinePopover";

// Isso aqui seria o array vindo do seu parser!
const steps = [
  { name: "Initiator", status: "completed" },
  { name: "Validations", status: "completed" },
  {
    name: "Approvals and Restrictions",
    status: "pending",
    internalSteps: [
      { name: "Approvals and Restrictions: Acceptance", status: "completed" },
      { name: "Approvals and Restrictions: Compliance", status: "pending" },
      { name: "Approvals and Restrictions: Fraud", status: "waiting" },
      // ... outros substeps
    ]
  },
  { name: "Restriction Removal", status: "waiting" },
];

const MyTimeline = () => (
  <Timeline.Root>
    <Timeline.Scroll>
      {steps.map((step, idx) => (
        <Timeline.Step key={idx} status={step.status}>
          <Timeline.Circle status={step.status} />
          <Timeline.
