// Onde você já tem algo como:
const feesActives = await FeeCase.find({
  cif: customerAccount[0].cifno,
  status: {
    $nin: ['DONE', 'REJECTED']
  },
  caseNumber: {
    $exists: true
  }
});

// Aqui você mapeia apenas caseNumber e id:
// result.feesActives = feesActives?.map((x) => ({
//   caseNumber: x.caseNumber,
//   id: x.id
// }));

// Para não quebrar o que já funciona, mantenha os campos originais e
// simplesmente acrescente os outros campos necessários (status, reason, etc.):
result.feesActives = feesActives?.map((x) => ({
  // Mantém o que já existe:
  caseNumber: x.caseNumber,
  id: x.id,

  // Acrescenta novos campos:
  status: x.status,    // e.g. "INITIAL", "PENDING_APPROVAL", etc.
  reason: x.reason,    // texto de justificativa
  caseId: x.caseId,    // se precisar da identificação do caso
  // Adicione outros campos do FeeCaseSchema conforme necessário
}));

// Pronto, sem mudar nada no fluxo principal, 
// você agora retorna também os dados extras que o front precisa.
return Result.ok(result);
