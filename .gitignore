const _returnObj = listFee.reduce((acc, element) => {
  let group = 'Fee Package';

  if (element.groupName !== 'ERA' && element.groupName !== 'EFRA') {
    acc.push({
      group,
      package: [
        { name: element.groupName },
        { id: element.id }
      ]
    });
  }

  return acc;
}, []); // Inicializa com um array vazio

console.log(_returnObj);
