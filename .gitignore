<CancelRequestModal
  open={cancelRequestIsOpen}
  onClose={() => setCancelRequestIsOpen(false)}
  onSubmit={(reason) => {
    if (data?.requestId) handleCancelRequest(data.requestId, reason);
  }}
  requestId={data?.requestId ?? ""}
/>



function handleCancelRequest(requestId: string, reason: string) { ... }
// para
function handleCancelRequest(requestId: string | undefined, reason: string) {
  if (!requestId) return;
  // ...
}
