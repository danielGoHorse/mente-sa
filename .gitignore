const _returnObj = listFee.map(element => {
  let group = 'Fee Package';

  if (element.groupName !== 'ERA' && element.groupName !== 'EFRA') {
    return {
      group,
      package: [
        { name: element.groupName },
        { id: element.id }
      ]
    };
  }

  // Retorna null ou um valor específico para os casos que não atendem a condição
  return null;
}).filter(item => item !== null); // Remove os itens nulos, se necessário

console.log(_returnObj);
