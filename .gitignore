// Cria uma chave única para cada fee usando a combinação de CIFNO, ACCTNO, FeeCode e FeeType
const uniqueFeesData = Array.from(
  new Map(feesData.map(row => [
    `${row.CIFNO}-${row.ACCTNO}-${row.FeeCode}-${row.FeeType}`, row
  ])).values()
);




// Substitua:
const feesData = await this.#getFeesFromSQL(customerAccount[0].cifno);
// Por:
const feesDataRaw = await this.#getFeesFromSQL(customerAccount[0].cifno);
const feesData = Array.from(
  new Map(feesDataRaw.map(row => [
    `${row.CIFNO}-${row.ACCTNO}-${row.FeeCode}-${row.FeeType}`, row
  ])).values()
);
